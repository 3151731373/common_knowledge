隔离性比想象的要复杂

（1）read uncommitted(未提交读)
事务中的修改即使没有提交，对其他事务也是可见的
脏读，会导致很多问题，很少使用。

（2）read committed(提交读)
事务中的修改提交后，才对其他事务可见
使用较多

（3）repeatable read(可重复读)
同一个事务中多次读取同样的记录，结果是一致的
mysql默认事务隔离级别

（4）serializable(可串行化)
最高的隔离级别，强制事务串行执行，避免幻读。
读取的每一行都加上锁，可能导致大量的超时和锁争用，很少使用

幻读：当某个事务在读取某个范围的记录时，另外一个事务又在该范围插入了新的记录，当之前的事务再次读取该范围的记录时，会产生幻行。
