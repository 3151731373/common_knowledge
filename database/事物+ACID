#事务<br/>
一组原子性的SQL查询/一个独立的工作单元/并发控制的基本单元，不可分割的工作单位<br/>
<br/>
#ACID<br/>
原子性（atomicity）:对于一个事务来说，不可能只执行其中的一部分操作。<br/>
一致性（consistency）:数据库总是从一个一致性的状态转换到另一个一致性的状态，合法的数据才可以被写入。<br/>
隔离性（isolation）:一个事务所做的修改在最终提交以前，对其他事务是不可见的。<br/>
持久性（durability）:一旦事务提交，其所做的更改会永久性的保存到数据库中<br/>
