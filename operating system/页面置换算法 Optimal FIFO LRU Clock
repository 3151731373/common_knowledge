页面置换算法 :在进程运行过程中，若其访问的页面不在内存，则需要把他们调入内存，但内存已无空闲空间时，则需要进行页面交换
抖动：频繁更换页面，刚调出的页面又很快被访问。

Optimal ：最佳置换算法（无法实现）
选择淘汰的页面是以后永远不实用的，或者最长时间内不被访问的。

FIFO ：先进先出，会产生Belady异常，所分配的物理块数增大，而页故障数不减反增的现象。

LRU ：最近最少使用，移除最长时间未被访问的页面（需要较多硬件支持）

LFU：最少使用，移除一段时间内访问频率最少的页面

Clock：按FIFO检查页面，若访问位为1则置0，如果为0则置换
